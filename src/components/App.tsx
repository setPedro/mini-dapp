"use client"

import Button from "./UI/Button"

export default function App() {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <Button onClick={() => { }} size="lg" color="primary">Connect your wallet</Button>
    </div>
  )
}

